import { escapeHtml } from './utils.js';

export function renderDashboard(lockState, auditTrail = [], testIdentity = null) {
  const currentStatus = escapeHtml(lockState.CurrentStatus);
  const statusColors = { PENDING: 'bg-zinc-500/20 text-zinc-400 border-zinc-500/40', APPROVED: 'bg-amber-500/20 text-amber-400 border-amber-500/40', REJECTED: 'bg-rose-500/20 text-rose-400 border-rose-500/40', EXECUTING: 'bg-blue-500/20 text-blue-400 border-blue-500/40 animate-pulse', EXECUTED: 'bg-emerald-500/20 text-emerald-400 border-emerald-500/40', FAILED: 'bg-red-600/20 text-red-500 border-red-600/40' };
  const statusColor = statusColors[currentStatus] || statusColors.PENDING;
  const lockId = escapeHtml(lockState.LockId);
  const commitHash = escapeHtml(lockState.GitCommitHash);
  const payloadHash = escapeHtml(lockState.FrozenPayloadHash);

  let authInputs = '';
  if (testIdentity) {
    authInputs = `<input type="hidden" name="devPubKey" value="${escapeHtml(testIdentity.pubKey)}"><input type="hidden" name="devSignature" value="${escapeHtml(testIdentity.signature(currentStatus, 'APPROVED', payloadHash))}"><input type="hidden" name="devSignatureReject" value="${escapeHtml(testIdentity.signature(currentStatus, 'REJECTED', payloadHash))}"><input type="hidden" name="devSignatureExecute" value="${escapeHtml(testIdentity.signature(currentStatus, 'EXECUTING', payloadHash))}">`;
  }

  return `<!DOCTYPE html><html lang="en" class="h-full bg-slate-950 text-slate-100"><head><meta charset="UTF-8"><title>AURA Edge Pod</title><script src="https://unpkg.com/htmx.org@1.9.10"></script><script src="https://cdn.tailwindcss.com"></script></head><body class="h-full flex flex-col justify-between font-sans antialiased" id="dashboard-container"><header class="border-b border-slate-800 bg-slate-900/50 backdrop-blur-md px-6 py-4"><div class="max-w-7xl mx-auto flex justify-between items-center"><div class="flex items-center gap-3"><div class="w-3 h-3 rounded-full bg-emerald-500 animate-pulse"></div><h1 class="text-xl font-bold text-emerald-400">AURA EDGE POD</h1></div><span class="text-xs font-mono text-slate-500">COMMIT: ${commitHash}</span></div></header><main class="flex-grow max-w-7xl w-full mx-auto px-6 py-8 grid grid-cols-1 lg:grid-cols-3 gap-8"><section class="lg:col-span-1 bg-slate-900 border border-slate-800 rounded-xl p-6"><h2 class="text-lg font-semibold text-slate-300 mb-4">TrustGate State</h2><div class="border border-dashed border-slate-800 rounded-lg p-6 text-center mb-6"><span class="inline-flex px-4 py-1.5 rounded-full text-sm font-semibold border ${statusColor}">${currentStatus}</span></div><div class="mb-6 text-sm text-slate-400 font-mono"><span class="block text-xs uppercase text-slate-500 mb-1">Payload Hash</span><span class="break-all bg-slate-950 px-2 py-1 rounded border border-slate-800 block text-xs" title="${payloadHash}">${payloadHash}</span></div><form id="transition-controls" class="space-y-3"><input type="hidden" name="payloadHash" value="${payloadHash}">${authInputs}<div class="grid grid-cols-2 gap-2"><button hx-post="/trustgate/transition?target=APPROVED" hx-target="#dashboard-container" hx-swap="outerHTML" ${currentStatus !== 'PENDING' ? 'disabled' : ''} class="px-4 py-2 bg-emerald-600 hover:bg-emerald-500 disabled:opacity-50 text-white rounded-lg text-sm">APPROVE</button><button hx-post="/trustgate/transition?target=REJECTED" hx-target="#dashboard-container" hx-swap="outerHTML" ${currentStatus !== 'PENDING' ? 'disabled' : ''} class="px-4 py-2 bg-rose-600 hover:bg-rose-500 disabled:opacity-50 text-white rounded-lg text-sm">REJECT</button><button hx-post="/trustgate/dispatch" hx-target="#dashboard-container" hx-swap="outerHTML" ${currentStatus !== 'APPROVED' ? 'disabled' : ''} class="col-span-2 px-4 py-2 bg-blue-600 hover:bg-blue-500 disabled:opacity-50 text-white rounded-lg text-sm mt-2">DISPATCH JOB</button></div></form></section><section class="lg:col-span-2 bg-slate-900 border border-slate-800 rounded-xl p-6"><h2 class="text-lg font-semibold text-slate-300 mb-4">Audit Trail</h2><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-800 text-sm"><tbody class="divide-y divide-slate-800/50 text-slate-300 font-mono">${auditTrail.length === 0 ? `<tr><td class="py-8 text-center text-slate-500">No transactions recorded.</td></tr>` : auditTrail.map(audit => `<tr><td class="py-3 px-4 text-slate-500 text-xs">${escapeHtml(audit.AuditId).slice(0, 8)}...</td><td class="py-3 px-4"><span class="text-slate-500">${escapeHtml(audit.PreviousStatus)}</span> &rarr; <span class="text-slate-200">${escapeHtml(audit.NewStatus)}</span></td><td class="py-3 px-4 text-xs text-cyan-400">${escapeHtml(audit.TriggeredBy).slice(0, 12)}...</td><td class="py-3 px-4 text-xs text-slate-400">${escapeHtml(new Date(audit.Timestamp).toLocaleTimeString())}</td></tr>`).join('')}</tbody></table></div></section></main></body></html>`;
}
